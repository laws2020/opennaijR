# List cached opennaijR datasets

Lists all datasets currently cached by opennaijR.

## Usage

``` r
naijr_cache_list()
```

## Value

Character vector of cached pin names.

## Examples

``` r
if (FALSE) { # \dontrun{
naijr_cache_list()
} # }
```
