% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbn.R
\name{cbn}
\alias{cbn}
\title{Access Central Bank of Nigeria (CBN) datasets}
\usage{
cbn(
  dataset,
  variables = NULL,
  from = NULL,
  to = NULL,
  filters = list(),
  raw = FALSE,
  canonical = TRUE,
  refresh = FALSE
)
}
\arguments{
\item{dataset}{Character scalar. Name or alias of the CBN dataset to retrieve
(e.g. \code{"inflation_ng"}, \code{"exchange_rate"}).}

\item{variables}{Character vector or \code{NULL}. Optional subset of variables
(columns) to return. If \code{NULL}, all available variables are returned.}

\item{from, to}{Date or character coercible to \code{Date}, or \code{NULL}. Optional date
range for time-series datasets.}

\item{filters}{Named list. Optional key-value filters applied at the query
definition stage.}

\item{raw}{Logical. If \code{TRUE}, returns parsed data immediately after payload
interpretation, skipping canonicalization.}

\item{canonical}{Logical. If \code{TRUE} (default), applies dataset-specific
canonicalization when a standardization function is defined in the manifest.}

\item{refresh}{Logical. If \code{TRUE}, forces a fresh download by bypassing any
cached version of the dataset. Default is \code{FALSE}.}
}
\value{
A data frame (or tibble) containing:
\itemize{
\item Canonicalized data if \code{canonical = TRUE}
\item Parsed raw data if \code{canonical = FALSE} or \code{raw = TRUE}
}
}
\description{
\code{cbn()} provides a unified interface for retrieving datasets published by the
Central Bank of Nigeria (CBN). It supports dataset aliasing, variable selection,
date filtering, optional canonicalization, and access to raw or parsed data.

The function follows a strict pipeline:
\enumerate{
\item Define query intent
\item Dispatch request (raw fetch only)
\item Interpret payload (parse)
\item Canonicalize (optional)
}
}
\details{
Canonicalization standardizes column names, data types, and structures across
datasets to ensure consistency for analysis and downstream integration.

When \code{raw = TRUE}, canonicalization is bypassed regardless of the value of
\code{canonical}.
}
\examples{
\dontrun{
cbn("inflation_ng")
cbn("exchange_rates", canonical = FALSE)
cbn("exchange_rates", raw = TRUE)
}

}
\seealso{
define_query(), dispatch_request(), interpret_payload(),
canonicalize_data()
}
